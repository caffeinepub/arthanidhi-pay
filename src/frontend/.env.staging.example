# Staging Environment Configuration
# Copy this file to .env.staging for staging environment: cp .env.staging.example .env.staging
# Run with: npm run dev -- --mode staging

# Backend Mode Configuration
# Set to 'ic' for Internet Computer canister backend
# Set to 'rest' for external REST API backend
VITE_BACKEND_MODE=rest

# REST API Base URL (only used when VITE_BACKEND_MODE=rest)
# For staging, this should point to your staging backend server
# Replace with your actual staging backend URL
VITE_REST_BASE_URL=http://localhost:3001/api

# Staging-specific settings
# Add any staging-specific environment variables below
# Example: VITE_ENABLE_ANALYTICS=true
