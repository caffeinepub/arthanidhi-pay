# Development Environment Configuration
# Copy this file to .env for local development: cp .env.development.example .env

# Backend Mode Configuration
# Set to 'ic' for Internet Computer canister backend (recommended for full-stack development)
# Set to 'rest' for external REST API backend (Node.js/Express in backend-node folder)
VITE_BACKEND_MODE=ic

# REST API Base URL (only used when VITE_BACKEND_MODE=rest)
# For local development, this should point to your local Node.js/Express server
# Example: http://localhost:3001/api
VITE_REST_BASE_URL=http://localhost:3001/api

# Development-specific settings
# Add any development-specific environment variables below
# Example: VITE_ENABLE_DEBUG_LOGS=true
